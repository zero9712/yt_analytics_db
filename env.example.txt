# ─────────────────────────────────────────────────────
#  yt_notion_sync  —  environment variables
#  Copy this file to .env and fill in the values.
#  Never commit .env to version control.
# ─────────────────────────────────────────────────────

# Notion internal integration token
# https://www.notion.so/my-integrations  →  copy "Internal Integration Token"
NOTION_TOKEN=secret_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Google OAuth client secret file path (relative to project root)
# Download from Google Cloud Console → APIs & Services → Credentials
GOOGLE_CLIENT_SECRET_PATH=client_secret.json

# Google OAuth token cache file (auto-created on first run)
GOOGLE_TOKEN_PATH=token.json
